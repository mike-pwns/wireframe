#include <stdio.h>

// customizers
int FPS; // will run loop so updates for this fps; js will overclock but end result will be this fps.
float FOV; // will change distance to fit that fov in the 1:1 canvas 


int main() {


  return 0;
}
